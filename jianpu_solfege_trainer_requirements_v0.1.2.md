# 简谱唱名训练器：完整开发需求 0.1.2

**简谱识读 + 相对音程，两个独立模块，各含练习与考试；保留简谱考后自选强化及 Apple 风格视觉规范。**

版本日期：2026-09-26。基于 `jianpu_solfege_trainer_requirements_v0.1.0.md` 增量修订，本文是完整需求，可以独立执行。0.1.0 文件与配套视觉文件原样归档；其正文中的历史 v1.2 标识不回写。本版产品/文档版本为 0.1.2，存储版本单独定义为 schema v3，两者不混用。

## 0. 执行范围与版本差异

实现实际网页和已有 Mac 应用，不只交付设计描述。沿用 TypeScript、Vite、Tone.js、AppKit/WKWebView，不重建无关功能，不增加登录、云服务、AI、录音或大型框架。

本版新增与变化：

- 首页的两个主入口改为“简谱识读”“相对音程”；进入模块后再选“练习模式 / 考试模式”。
- 简谱识读沿用原有数字题面、音乐映射、计分、均衡出题、考试反馈与 21 题强化规则。
- 相对音程每题先播放一次当前调的参考 do，再播放一个随机目标音；目标包含 do、re、mi、fa、sol、la、si，全程不显示目标音符数字。
- 用户已明确确认：参考 do **每题播放**，并非每轮只播放一次。重听也始终播放同一对“do → 目标音”。
- 相对音程的练习即时反馈，考试交卷后统一反馈。音频是作答依据，静音时不能继续猜题计分。
- 两模块各自保存调性、练习题数、考试题数和上次模式；音量/静音是全局声音偏好。报告和历史清楚标记模块，不合并不同任务的成绩或速度。
- 保留简谱识读的考后自选强化。相对音程本版提供练习、考试、逐唱名报告和实际混淆方向，不新增听音强化流程。
- 升级到 schema v3，支持旧 schema v1/v2 备份和迁移，不删除旧历史、不伪造原来没有的答案。
- 第 15 节延续浅色、系统字体、单一蓝色、留白与克制圆角；第 12.5、12.6 节为视觉和新模块验收。

## 1. 产品目标与运行方式

本程序包含两个能力目标不同的模块：

| 模块 | 用户任务 | 声音作用 | 不能做的解释 |
| --- | --- | --- | --- |
| 简谱识读 | 看到数字，选首调唱名。例如看到 5 选 sol | 辅助建立数字、唱名与当前调音高的联系 | 不能把识读成绩叫作听力水平 |
| 相对音程 | 先听 do，再听一个目标音，从七个唱名中识别目标 | 必需的作答依据 | 不测绝对音高，不让用户选“大三度”等音程名称，不评估跟唱 |

“相对音程”在本版具体指相对于调内主音的单音唱名听辨，不包含任意两个随机音之间的音程类别测验。

界面使用简体中文。支持本地网页和已实现的 macOS 应用；现有 Apple Silicon Mac 应用需随功能更新重新打包，内置页面、八份钢琴采样和许可，无需 Node.js 或联网即可运行。Mac 最低目标版本为 macOS 13，运行验收的平台须如实记录；不要求 Intel、App Store 或自动更新。

本地网页使用 Mac Chrome 或 Safari，通过本机 HTTP 服务运行。无需 DAW、MIDI 键盘、麦克风、账户或付费 API。安装依赖和准备采样可联网，日常使用不请求外部字体、CDN 或云服务。Mac 与浏览器存储独立，不自动读取或迁移浏览器私有数据。

## 2. 音乐规则：不可擅自改变

### 2.1 使用首调唱名，不使用固定唱名

数字与唱名的映射，在本程序支持的所有大调中都保持不变：[R1]

| 简谱数字 | 1 | 2 | 3 | 4 | 5 | 6 | 7 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 唱名 | do | re | mi | fa | sol | la | si |

统一使用小写 `sol` 和 `si`，不把 `so`、`ti` 作为额外选项。不同教材存在拼写习惯差异，本项目只采用上述一套标签。

**换调改变实际播放音高，不改变数字到唱名的正确答案。**

例如在 D 大调中：`1 → do → D4`，而不是因为实际音名是 D 就把答案改成 re。

### 2.2 本次版本只支持大调及不同调高

用户所说的 C 调、D 调、E 调，在本版明确实现为 C 大调、D 大调、E 大调等。不要把“切换调高”与“大调改成小调”混为一谈。

本次版本提供十二个大调选项，使用下表的写法。小调、教会调式、固定唱名、带升降号的简谱音符、高低八度点和休止符不在本版范围。

### 2.3 MIDI 音高规则

本项目规定中央 C 为 `C4 = MIDI 60`，`A4 = MIDI 69 = 440 Hz`，使用十二平均律。

这里只借用 MIDI 音符编号作为内部数据，不需要生成或读取 `.mid` 文件。

大调相对主音的半音偏移为：[R1]

```ts
const MAJOR_OFFSETS = [0, 2, 4, 5, 7, 9, 11] as const;
// degree 必须是 1..7 的整数。
// tonicMidi 来自下面的调性配置。
const midi = tonicMidi + MAJOR_OFFSETS[degree - 1];
const frequencyHz = 440 * 2 ** ((midi - 69) / 12);
```

频率换算采用标准 MIDI 音高换算关系。[R7]

注意：不能每级简单加一个半音；不能在 D、E 等调仍然只使用钢琴白键；不能把最终 MIDI 值对 12 取模而丢失八度信息。

### 2.4 完整调性配置

为避免初期八度选择过多，本次版本把每个调的 `1` 放在下表指定位置，并由此向上排列七个音。

| 调性显示 | tonicMidi | 1—7 对应的实际音名 |
| --- | ---: | --- |
| C 大调（1=C） | 60 | C4 D4 E4 F4 G4 A4 B4 |
| D♭ 大调（1=D♭） | 61 | D♭4 E♭4 F4 G♭4 A♭4 B♭4 C5 |
| D 大调（1=D） | 62 | D4 E4 F♯4 G4 A4 B4 C♯5 |
| E♭ 大调（1=E♭） | 63 | E♭4 F4 G4 A♭4 B♭4 C5 D5 |
| E 大调（1=E） | 64 | E4 F♯4 G♯4 A4 B4 C♯5 D♯5 |
| F 大调（1=F） | 65 | F4 G4 A4 B♭4 C5 D5 E5 |
| F♯ 大调（1=F♯） | 66 | F♯4 G♯4 A♯4 B4 C♯5 D♯5 E♯5 |
| G 大调（1=G） | 67 | G4 A4 B4 C5 D5 E5 F♯5 |
| A♭ 大调（1=A♭） | 68 | A♭4 B♭4 C5 D♭5 E♭5 F5 G5 |
| A 大调（1=A） | 69 | A4 B4 C♯5 D5 E5 F♯5 G♯5 |
| B♭ 大调（1=B♭） | 70 | B♭4 C5 D5 E♭5 F5 G5 A5 |
| B 大调（1=B） | 71 | B4 C♯5 D♯5 E5 F♯5 G♯5 A♯5 |

这些音名由上一节的规则推导而来。播放以 MIDI 编号为准，展示时按调性保留正确的升降号拼写。例如 F♯ 大调的第七级应显示 E♯，即使它和 F 在本程序的十二平均律中同音。

`1=C` 本身不指定八度；把 1 放在 C4 是本程序的练习音区约定。设置区可以另显示“本轮起始音：C4”，不要把 C4 约定解释成简谱的普遍规定。

### 2.5 相对音程的参考音与目标范围

参考 do = 第 2.4 节当前调的 tonicMidi。目标音 = tonicMidi + MAJOR_OFFSETS[degree - 1]，degree 为 1—7。参考音位于该组七音的最低端，不额外降低一个八度；目标 do 与参考 do 同音高，仍必须清楚播放两次。目标不包含高八度 do。

例如 C 大调：C4 → C4/D4/E4/F4/G4/A4/B4；D 大调：D4 → D4/E4/F♯4/G4/A4/B4/C♯5。F♯ 大调目标 si 为 E♯5 / MIDI 77。两个模块使用同一套首调音乐规则，不能复制出另一套白键或固定唱名算法。

## 3. 模块、模式与设置

### 3.1 主流程

```text
首页
├─ 简谱识读
│  ├─ 练习模式 → 设置 → 数字题面 + 单音 → 即时反馈 → 小结
│  └─ 考试模式 → 设置 → 数字题面 + 单音 → 中性确认 → 报告
│                                             └─ 主动选强化 → 对照复习 → 21 题强化 → 小结
├─ 相对音程
│  ├─ 练习模式 → 设置 → 每题 do + 目标音，无数字 → 即时反馈 → 小结
│  └─ 考试模式 → 设置 → 每题 do + 目标音，无数字 → 中性确认 → 报告
├─ 认识音符：独立自由试听，不计分
└─ 历史记录：按模块、模式筛选并打开详情
```

“模块”与“模式”是两个独立字段。不得把相对音程塞进 practice/exam/reinforcement 枚举，不得把进入相对音程实现成原模式里一个随时切换的复选框。

### 3.2 反馈、出题与记录

| 项目 | 练习 | 考试 | 简谱强化 |
| --- | --- | --- | --- |
| 模块 | 两模块均有 | 两模块均有 | 仅简谱识读 |
| 提交反馈 | 立即揭晓 | 仅“答案已记录，请继续下一题。” | 同简谱练习 |
| 进行中正确数 | 可显示 | 禁止显示 | 可显示 |
| 题数 | 默认 21；7/21/35 | 默认 35；7/21/35 | 固定 21 |
| 出题 | 七音均衡洗牌袋 | 同左，预先生成，不因错答改题 | 14 目标 + 7 全范围 |
| 历史 | 独立练习记录 | 独立考试记录 | 独立强化记录，不改原考试 |

两个模块均为七个随机唱名按钮，不做二选一，不编号、不绑定颜色，不因模式改变答案标签。普通练习/考试 7、21、35 题每个目标级数恰好 1、3、5 次，跨袋不相邻重复；题序与选项顺序独立随机。

### 3.3 设置边界

- 两模块分别保存调性、练习题数、考试题数和上次主模式，彼此不覆盖。新相对音程默认 C 大调、练习 21、考试 35。
- 音量默认 30%，0—100%，静音偏好全局共享。相对音程开始时若静音/音量为 0，必须明确提示和等待用户启用；不静默改声音设置。
- 相对音程的启用按钮在音量为 0 时明确标“启用声音（30%）并继续”，用户点击后可恢复 30%；已有正音量仅取消静音。
- 开始后锁定模块、模式、调性、题数和强化目标；切换必须先结束当前轮次。音量可随时调整；听音过程中静音会暂停本题。
- 7 题考试保留小样本提示，不阻止使用。不新增限时、等级、通过线或排名。

## 4. 题目、播放与状态

### 4.1 简谱识读

沿用 0.1.0：中央大数字，自动播放对应单音一次，七个唱名选项。作答前不显示正确唱名、实际音名、答案表或琴键。题目可作答时开始计时；主动静音可以继续，但须显示并记录声音条件。

### 4.2 相对音程

首次点击开始，等待音频就绪，然后每题依次执行：

1. 生成并保存目标级数和七项顺序，渲染中性题区“听音选择唱名”。不显示目标数字、目标音名、正确唱名或区间大小。
2. 播放参考 do，约 0.8 秒持键 + 0.18 秒释放。
3. 参考音自然释放后留约 0.25 秒间隔，再播放目标音，同样约 0.8 秒持键 + 0.18 秒释放。
4. 整对声音播放完成后才启用答案并开始操作计时。播放过程中显示“正在播放参考 do 与目标音…”，答案和重听暂不可操作。
5. 接受首次选择并锁定答案；练习揭晓目标唱名和实际音名，考试只给中性确认。反馈不显示目标音符数字。
6. 自动重播一遍本题的参考 do 与目标音，完成后用户可点击下一题；新题再次播放参考 do 与新目标音，最后一题在答后重播完成后结算。

参考与目标音的音色、力度、持键、释放保持一致，无背景音乐或奖惩音。不得把两音叠成和弦；do-do 同音题也必须有间隔的两次发声。内部可保留 degree/MIDI 用于出题与统计，但不把这些字段写入未作答的 DOM、data 属性、title 或 ARIA 标签。

### 4.3 重听与计分

首次选答后，不论答对或答错，都自动再播放当前题一次：简谱识读播放该数字对应的音，相对音程播放完整参考 do → 目标音；不得播放用户误选项对应的音。适用于练习、考试和简谱强化。反馈立即显示，自动重播结束才启用下一题/交卷，答案与重听在播放期间锁定。自动播放不增加手动重听次数，不改变首次答案、计分或已记录耗时；简谱主动静音时保持静音并允许继续。

两个模块作答前后都能重听，保持原题、调性、目标、选项顺序和题号。简谱重听单音；相对音程重听完整 do → 目标音，不能只重听目标，也不播放误选项。

重听不重置计时、不扣分、不改首次答案。相对音程重听期间暂时锁定答案、重听和下一题，完成后恢复原作答/反馈阶段。快速连续点击不排队、不叠音；结束、暂停或切换题目必须取消未触发的声音与完成回调。

正确率 = 首次答对数 / 已答数；未答不算错。提前结束标明部分完成，零作答显示“—”。结果按唯一 session ID 保存一次。

键盘使用 Tab、Shift+Tab 和原生空格/Enter 激活，忽略长按重复。禁止“按 1 选 do”等数字捷径。新题焦点移到数字标题或听音标题，不能落在正确选项；一次 Enter 不能既提交又进入下一题。

### 4.4 暂停、计时与声音异常

使用单调时钟。简谱计时口径保持不变；相对音程从首遍完整双音播放结束才起算，之后包括判断、查找选项、主动重听和操作时间，不宣称纯听觉反应时间，也不和简谱耗时直接比较。

浏览器后台、Mac 切换/隐藏/最小化、音频意外挂起或播放失败：停止当前声音、取消未来目标音、暂停本题，不判错。等待用户点击继续：未答题保留原目标并从参考 do 重播；已答题恢复已提交状态，不能重答；若中断发生在答后播放期间，继续时从头重播当前单音或整对声音，完成后开放下一题。未答题在播放中或作答中被中断，reactionMs 置 null；已提交题不追溯改耗时。

简谱音频错误可重试或明确“静音继续”。相对音程不能静音继续；静音、音量 0 或音频失败须恢复声音再继续，用户也可提前结束。不能用数字提示、虚假成功或自动跳题补偿无声。

### 4.5 辅助视图

认识音符显示完整数字—唱名映射、当前调音名，可自由试听，不计分；不能从正在答题的轮次直接打开答案表。相对音程不会因为隐藏数字而移除七个唱名选项或必要操作。

### 4.6 状态约束

区分 loading、listening、answering、practiceFeedback、examAnswerRecorded、paused、audioError、result。listening 是相对音程的双音播放阶段，以及简谱已答题的重播阶段；暂停必须保留其返回阶段。加载/播放未结束时不能提交。旧加载或播放回调不能解除新的暂停、推进新题或覆盖已结束的轮次。

原有设置、历史、确认弹窗和强化复习状态保持有效。进行中的轮次不自动恢复；刷新/重开回首页，已结束记录保留。Mac 关闭进行中轮次时保持明确退出确认。

## 5. 练习模式

本节的数字题面示例用于简谱识读。相对音程沿用首次计分和即时反馈时机，文案为“你选择了 fa；目标音的正确唱名是 sol。”，次行可揭晓参考与目标实际音名，不显示目标数字。

采用第 6.2 节的均衡出题规则。选择后立即显示正误、正确的数字—唱名关系及当前实际音高，例如：

> 你选择了 fa；正确答案是 sol。5 = sol，本轮 D 大调中实际音是 A4。

不要求答错后必须再次点对才能继续；首次提交仍是错误。作答阶段不额外发出有音高的奖励音、错误音或错误选项的音。

底部可显示已答、正确与错误数。练习结束显示小结：简谱识读显示逐数字统计，相对音程显示逐唱名统计，允许同设置再练。练习记录标为“练习”，不混入考试分数。

本版的自动混淆分析和强化推荐以考试报告为入口；不再为普通练习另造一套自动推荐流程。练习成绩不是正式考试成绩。

## 6. 考试模式与报告

### 6.1 不泄露即时答案

考试中提交答案后只显示：

> 答案已记录，请继续下一题。

所选按钮可以使用中性样式，七个按钮锁定。不能显示正确/错误文字、红绿高亮、正确音名、得分、连对次数或实时正确率，也不能通过提示音、动画、无障碍标签泄露对错。

考试组件不渲染正确答案解释；不能只是用 CSS 把解释藏起来却在可访问文本中保留。内部仍可计算正确性，但统一在交卷后展示。当前考试不能打开同一轮的答案详情。

这是本地自测，不需要实现防开发者工具作弊、防截屏或远程监考。

### 6.2 均衡出题

普通练习与考试采用洗牌袋：每袋包含 1—7 各一次，打乱后出题，用完再生成下一袋。跨袋同号相邻时调整新袋首项。

21 题时每个目标级数恰好 3 次，35 题时各 5 次，7 题时各 1 次；简谱以数字呈现，相对音程以声音呈现。题序与答案选项分别随机，考试开始后不根据当场的错题改变后续题目分布。

随机算法允许注入固定种子或随机源用于测试；生产环境不默认使用固定题序。

### 6.3 交卷与部分完成

最后一题提交后，点击“交卷并查看报告”完成考试。提前交卷需要确认，并说明未作答题不会计为错误，但本轮将标为“提前结束 / 部分完成”。

正确率统一使用 `首次答对数 / 已作答数`；同时显示 `已答数 / 计划题数`，不把 1 题答对的提前交卷显示成“满分完成 35 题”。本版不额外生成另一套按计划题数计分的百分制分数。

零作答显示“暂无作答”，正确率与速度显示“—”，不生成混淆结论或强化入口。

### 6.4 考试报告

报告标题附近必须显示模块。以下逐数字/题目数字描述用于简谱；相对音程使用“逐唱名统计”、目标唱名与“听到 mi 时选成 fa”的表述。相对音程的目标实际音名仅交卷后揭晓。

报告至少包含：

- 调性、完成状态、已答/计划题数、正确/错误数、正确率。
- 首次答对且未中断题目的操作耗时中位数；没有有效样本则显示“—”。
- 重听情况、是否曾静音或中断，避免把不同条件下的速度当成可直接比较的成绩。
- 七个目标级数各自的作答数、正确数、错误数、错误率；简谱按数字标示，相对音程按唱名标示；未作答的项目显示“无样本”。
- 每道已答题的目标（简谱显示数字，相对音程显示目标唱名）、用户所选唱名、正确唱名；错误列表可直接查看。
- 第 7 节的混淆明细；仅简谱识读包含第 8 节的可选强化区。

可展开显示 7×7 混淆表，但默认应优先展示容易读懂的文字，例如“看到 3（mi）时，2 次选成 fa”。

报告中的按钮：“同设置重新考试”“返回首页”；简谱识读有错误且有详细记录时，还提供“选择强化项”。支持从历史中重新打开简谱报告再选择强化。

## 7. 易混淆项识别：统计实际错选，不猜测原因

### 7.1 数据来源与混淆矩阵

只使用当前模块、本次已交卷考试的首次有效答案。不得把普通练习、强化练习或其他考试的数据混入本次报告。

将唱名按固定映射转回其级数：`do→1 ... si→7`。该转换与答案按钮的屏幕位置无关。

定义 `M[i][j]` 为“目标级数为 i，实际选择了对应级数 j 的唱名”的次数。简谱以数字表示目标，相对音程以声音表示同一调内级数；两者共享此数学表示，但数据不可跨模块合并。可使用长度 7 的二维数组，代码需明确 0-based 下标与 1—7 的转换。

- `M[i][i]` 是正确次数，不是混淆。
- `M[i][j] > 0 且 i ≠ j` 才是实际发生过的错误方向。
- 未答题没有选择记录，不计入矩阵任何一格。

每个数字的作答次数为所在行总和；错误次数为该行非对角线总和；错误率为错误次数除以作答次数。分母为零时返回 `null`。

### 7.2 混淆组与证据强弱

把一对数字 a、b 组成候选组时，使用：

```text
pairCount(a, b) = M[a][b] + M[b][a]，且 a < b
```

只列出 `pairCount > 0` 的组，按次数从高到低排序；次数相同时按 a、b 升序，保证结果稳定。每组必须保留两个方向各自的数量，不把单向错误伪装成“双向都经常错”。

例：出现两次 `3 → fa`，但 `4 → mi` 从未发生，应写：

> 3（mi）与 4（fa）值得对照巩固。本次看到 3 时，2 次选成 fa；反方向未发生错误。

不要写成“你经常把 3 和 4 互相搞错”。若 4 根本未作答，还须写“4 的作答样本为 0”，不能写成“4 已经掌握”。

提示标签规则是产品设计参数，不是心理学或统计诊断：

- 同组仅 1 次错误：标为“本次单次错误，可复习”，不宣称长期薄弱。
- 同组至少 2 次错误：标为“本次重复混淆”，仍提示只是本轮观察。
- 全对：显示“本次未发现错选项”，不推断永久掌握，不强行生成混淆。
- 答对但用时长：可以在耗时统计中呈现，但本版不因此自动加入混淆列表。

第一版不设置统计显著性、长期能力分级或固定“掌握阈值”。少量题目的观察只用于推荐复习。

### 7.3 分析示例与测试样本

假设一轮 C 大调 35 题，每个数字出现 5 次：

- 3 有 2 次选择 fa，其余 3 次正确。
- 5 有 1 次选择 la，其余 4 次正确。
- 其余数字全对。

则应得到 32 次正确、3 次错误；`M[3][4]=2`，`M[5][6]=1`（此处数学下标用数字 1—7，代码自行减 1）。

候选组为 `3/4` 和 `5/6`，分别显示“本次重复混淆”“本次单次错误”。不能因为 `3→fa` 出错而误把正确关系改写成 `3=fa`。

## 8. 简谱识读的考后自选强化练习

本节只适用于简谱识读。相对音程考试不能作为简谱强化来源，报告不显示本节的勾选与启动入口。原有历史强化及其来源快照保持可用。

### 8.1 用户是否进入，由用户决定

考试报告展示候选混淆组及其证据，每组提供复选框，默认勾选本轮实际发生过错误的全部组，用户可只选其中一部分。默认勾选不是自动开始。

显式操作包括：

- “开始所选项强化”：仅在至少选中一组时启用。
- “暂不强化 / 返回首页”：立即离开，不要求解释，不影响报告保存。

用户也可留在报告中继续查看，不必被弹窗催促。历史报告中的候选项保持可重新选择。全对、零作答、旧版缺少具体错选记录时，不显示可误启动的强化按钮。

### 8.2 强化对象与调性

将所选混淆组两端数字取并集，作为目标集合 T。例如选中 `3/4` 和 `5/6`，则 `T={3,4,5,6}`。

练两端是为了对照它们正确的映射，不意味着两端都已被证明答错。报告的错误率仍按真实记录计算。

强化默认并固定沿用来源考试的调性。想换调可返回首页新开练习或考试；本版不在同一强化轮次中自动变调。

强化计划生成时快照保存来源考试 ID、来源调性、候选组错误方向与次数、用户选中的组和目标数字集合。进入后不再因为切换历史页面而改变计划。

### 8.3 不计分的对照复习

开始强化后，先给出所选目标的正确映射，例如：

```text
3 = mi          4 = fa
5 = sol         6 = la

点击卡片试听当前调中的对应钢琴音。
[ 开始答题 ]    [ 跳过复习并开始答题 ]    [ 返回报告 ]
```

两种开始按钮进入同一个答题流程；可以合并为一个“开始答题”，配文说明复习可跳过。试听由用户主动点击，不自动循环，不计入题数与成绩。

正式强化作答时隐藏映射卡片，仍显示七个随机排列的唱名选项，不缩成二选一，不给正确选项额外提示。

### 8.4 强化出题：固定 21 题，不无限循环

每轮组成明确：**14 道目标强化题 + 7 道全范围复习题**。

全范围复习题中，1—7 每个数字各一次。它们与目标题混合出题，不把复习题标上能提示答案的标签。目标数字也包含在这 7 道题内，因此其实际比例会略高于 14/21。

14 道目标题的分配规则：

1. T 中每个数字先分配 1 道，保证两端都实际练到。
2. 对每个目标数字 d，权重 `w(d)` 为所有“已选且包含 d 的混淆组”的 `pairCount` 之和。未选中的组不参与计算。
3. 剩余 `14 - |T|` 道按上述正权重比例分配。先取各自配额的整数部分，余题按小数余数从大到小补齐；余数相同按数字升序，确保算法可复现。
4. 合并这 14 道与全范围 7 道，再随机排列，避免同数字连续。每题七个选项独立洗牌。

队列生成要有限次尝试并有回退，不能写可能永不结束的随机重试。使用有剩余次数约束的排列算法，或有限随机尝试后使用可行排列回退，保证数量不被“防重复”逻辑悄悄改变。

例如只选 `3/4`：14 道目标题中 3、4 各 7 道，加上全范围复习后，3、4 各出现 8 次，其他五个数字各 1 次，总数 21，且不能同号相邻。

若所有 1—7 都进入目标集合，则没有“非目标项”，仍按以上规则完成 21 题；不要尝试从空的非目标池抽题。

本版不因强化中再次答错就无限追加题目。21 题后正常结束，可以让用户自选再来一轮。

### 8.5 即时反馈与结果

强化的反馈和首次计分规则与普通练习一致。答错立即显示正确映射，重复点击或后续查看解释不把错误改成正确。

小结显示来源考试、目标数字、已答/计划题数、目标项与全部题目各自的正确次数/错误次数/正确率，以及本轮实际发生的错选方向。提前结束也可正常查看小结。

提供“按原选项再强化一轮”“返回原考试报告”“按来源考试设置重新考试”。再强化沿用原先选择的目标与配额规则，重新洗牌，不自动扩展成所有历史错题。

重新考试必须生成新的全范围均衡题序、获得新 ID 并另存考试记录，不复用错题强化队列，也不覆盖来源考试。

**强化即时给答案且出题分布不同，不能把其正确率直接减去原考试正确率，显示成能力提升百分比。** 可以分别列出两轮数据，并解释条件不同；如需重新测评，由用户主动发起新的考试。

强化结束不显示“已经永久掌握”“彻底消除混淆”之类结论，也不要求达到某阈值才能退出。

## 9. 钢琴声音与加载可靠性

### 9.1 音色实现

推荐使用 `Tone.js Sampler + 本地钢琴采样`。Sampler 可以根据标定音高对采样进行移调，不要求为每个目标音单独放一份录音。[R2]

特别注意 Tone.js 播放接口的数值音高参数通常表示 Hz，而不是 MIDI 编号。应先显式转换，例如 `Tone.Frequency(midi, "midi").toFrequency()`，或传入含八度的音名字符串；不能把 `60` 直接当作 C4 传给按 Hz 解释的接口。[R2]

最终验收必须有可辨识的钢琴音色，不能用正弦波蜂鸣或随意的合成器音色冒充钢琴。

音频实现抽象为模块，至少提供：初始化、播放 MIDI 单音、依次播放参考/目标双音、取消所有当前及待播声音、设置音量、获取加载/错误状态。双音播放须返回明确的完成或取消结果，停止时取消尚未触发的目标音和完成通知。UI 和出题逻辑不直接操作散落的音频节点。

单音按约 0.8 秒持键、再短暂自然释放处理；固定适中的力度，不随机变化音量或失谐。不同题目及重复重听之前要平滑停止旧声音，避免叠音或突然截断爆音。不要给每题叠加混响和背景音乐。

### 9.2 采样来源与交付

优先考虑 Tone.js 官方示例使用的 Salamander 钢琴采样。原始 Salamander Grand Piano 作者为 Alexander Holm，FreePats 列出的许可证为 CC BY 3.0；具体采用文件仍须核查来源与许可说明。[R2][R5]

也可使用许可明确、允许随项目分发的其他钢琴采样，例如 FreePats 的 Upright Piano KW 页面列明采用 CC0。[R5]

仅获取本项目需要的音区，不为七音训练器下载几百 MB 或数 GB 的完整音色库。采样需覆盖目标 MIDI 60—82，可使用附近的标定采样合理移调。

将实际采样放在 `public/audio/piano/` 或项目相应静态目录，运行时使用本地路径，不从第三方 CDN 临时加载。随项目提供 `THIRD_PARTY_NOTICES.md`，记录来源、作者、许可证、取得日期和裁剪/转码等改动；需要保留的许可文本也一并交付。

不提取或分发 GarageBand、Logic 或商业音源中的采样。无法获得合法可用采样时要明确报告阻碍，不静默替换为蜂鸣声并宣布钢琴功能完成。

建议提供可重复执行的资源准备脚本或采样清单；脚本不得在启动练习时重新联网下载。

### 9.3 浏览器音频限制

在用户点击“开始练习 / 开始考试 / 开始强化”的事件中调用 `Tone.start()` 或相应的 `AudioContext.resume()`，等待音频就绪后再播放。不能假设页面加载后可以自动发声。[R2][R3]

明确区分“采样加载中”“音频可用”“采样失败”“需要点击恢复声音”。加载失败提供重试，并显示有帮助的错误信息；不能在非用户主动静音的无声状态下继续自动出题计时。

若点击开始后仍被浏览器挂起，显示“点击启用声音”，保持未计分状态。浏览器或系统中断音频后恢复时也需处理。简谱识读中用户主动静音不视为加载失败，可以继续练习，但界面必须显示静音状态；相对音程静音必须暂停并等待明确启用声音；非主动静音的播放失败应暂停，不把未提交题算错。

所有异步加载、停止和切换都要避免旧任务回调影响新题。重复点击开始不能创建多个练习轮次；快速重听不能排队生成一串声音。

## 10. 本地保存、历史与兼容

### 10.1 保存范围

继续使用应用专用键 `jianpu-solfege-trainer`，结构升级为 schemaVersion: 3。保存全局声音设置、两个模块各自设置和最近 100 轮已结束记录（两模块总计 100，按结束时间排序、ID 去重）。记录含 module、mode、key、原始首次答案、完成状态、音频条件、摘要与可选强化来源。

历史列表每行显示“模块 · 模式 · 调性”、日期、完成状态、正确/已答；可分别按模块和模式筛选。重开报告、同设置再练/重新考试须沿用该记录的模块，而非当前界面最后一次选择的模块。

原考试、练习与强化记录彼此独立；不同模块不会合成一份成绩或混淆矩阵。强化来源淘汰后，原快照仍可读。本版只有简谱识读有强化，校验器拒绝相对音程强化或跨模块来源。

读写失败明确提示，保留内存会话；未知或损坏的旧键不能用新默认值覆盖。清空历史必须确认，处理本应用历史及迁移备份，不使用 localStorage.clear()。进行中轮次不保存。

### 10.2 schema v2 → v3

旧 v2 无 module 字段，来源是原简谱识读实现。迁移步骤：

1. 校验原设置、逐题答案、计分和强化来源；无法安全识别时停止迁移并保留原键。
2. 用 `jianpu-solfege-trainer:backup:v2` 保存原始字符串，已有该备份时不覆盖；原有 v1 备份也不覆盖。
3. 每条旧记录增加 module: 'notation'；保留 ID、模式、调性、日期、实际首次选择、耗时、中断和强化快照。派生 summary 按原首次答案校验重算，不改答案。
4. 旧调性、两种题数及上次模式放入 settings.modules.notation；音量/静音继续全局保存。settings.modules.relative 初始化为 C、21/35 题、practice。
5. 备份成功后才写 v3。备份或写入失败不覆盖原值，明确显示仅在内存中运行。

### 10.3 schema v1 与未知数据

旧 v1 摘要仍可迁移：原记录标 module: notation、mode: practice、legacySummaryOnly: true，answers: null；保留原摘要，不猜测错选。简谱设置保留，原无考试题数时默认 35。写入原有 `jianpu-solfege-trainer:backup` 备份后直接转 v3。

v3 必须有合法 module 和两套设置；未知 module 不默认为简谱。相对音程不能保存 mutedAtAnswer: true 的答案。原存储来源不变：Mac bundle ID `local.jianpu.solfege`、本机固定端口 41876 不因产品版本号变化；浏览器与 Mac 不自动互导记录。

### 10.4 类型契约

以下为本版实际数据结构。module 由 SessionConfig 和 SessionRecord 统领同一轮；内部 degree 仅是共享的调内音级表示，不必在每条答案冗余保存另一个 module。

```ts
export type Degree = 1 | 2 | 3 | 4 | 5 | 6 | 7;
export type Solfege = 'do' | 're' | 'mi' | 'fa' | 'sol' | 'la' | 'si';
export type KeyId = 'C' | 'Db' | 'D' | 'Eb' | 'E' | 'F' | 'F#' | 'G' | 'Ab' | 'A' | 'Bb' | 'B';
export type Mode = 'practice' | 'exam' | 'reinforcement';
export type ExerciseModule = 'notation' | 'relative';
export type QuestionCount = 7 | 21 | 35;
export interface ModuleSettings {
  key: KeyId; practiceQuestionCount: QuestionCount; examQuestionCount: QuestionCount;
  lastMainMode: 'practice' | 'exam';
}
export interface Settings {
  modules: Record<ExerciseModule, ModuleSettings>;
  volume: number; muted: boolean;
}
export interface Question {
  id: string; index: number; key: KeyId; degree: Degree; midi: number;
  displayNote: string; correctAnswer: Solfege; options: Solfege[];
}
export interface AnswerRecord {
  questionId: string; questionIndex: number; degree: Degree; midi: number;
  displayNote: string; selected: Solfege; correct: boolean; reactionMs: number | null;
  replaysBeforeAnswer: number; replaysAfterAnswer: number;
  interruptedBeforeAnswer: boolean; mutedAtAnswer: boolean;
}
export interface PerDegreeStats { answered: number; correct: number; wrong: number }
export interface Summary {
  answered: number; correct: number; wrong: number; medianCorrectReactionMs: number | null;
  perDegree: Record<Degree, PerDegreeStats>;
}
export interface ConfusionPair { a: Degree; b: Degree; aToB: number; bToA: number; total: number }
export interface ReinforcementSource {
  examId: string; examKey: KeyId; examEndedAt: string; examPlannedQuestions: QuestionCount;
  examAnswered: number; examCorrect: number; examEndedEarly: boolean;
  selectedPairs: ConfusionPair[]; targetDegrees: Degree[];
}
export interface SessionRecord {
  id: string; module: ExerciseModule; mode: Mode; key: KeyId; plannedQuestions: QuestionCount;
  startedAt: string | null; endedAt: string; endedEarly: boolean;
  audioEverMuted: boolean | null; audioEverInterrupted: boolean | null;
  answers: AnswerRecord[] | null; summary: Summary;
  reinforcementSource?: ReinforcementSource; legacySummaryOnly?: boolean;
}
export interface SavedData { schemaVersion: 3; settings: Settings; sessions: SessionRecord[] }
```

统计由本轮答案推导，不持久化第二套混淆矩阵。Mode 与 ExerciseModule 正交；强化只允许 module: notation。首次答案不可由重听、反馈或后续强化改写。

## 11. 推荐技术方案与工程要求

空仓库采用 `Vite + TypeScript + HTML/CSS + Tone.js`。已有 React/Vue 等前端仓库则沿用，不为新增模式重写整个项目。无需后端、数据库或机器学习服务。[R6]

根据实际依赖检查 Node.js 版本要求，采用兼容的稳定版本并提交锁文件，不无条件强装测试版。

建议模块：

```text
src/
  music.ts          # 唱名映射、调配置、MIDI 与展示音名
  quiz.ts           # 均衡题序、选项洗牌、通用首次提交
  session.ts        # 三种模式状态与暂停/结束/去重
  confusion.ts      # 混淆矩阵、有方向的错误、候选组
  reinforcement.ts  # 目标选择、来源快照、21 题配额与排题
  audio.ts          # 采样加载、播放、停止、音量、错误
  statistics.ts     # 正确率、逐数字统计、有效耗时中位数
  storage.ts        # schema v3、v1/v2 迁移、本地保存
  main.ts           # 页面与交互；复杂后可拆视图
  style.css
public/audio/piano/
tests/
README.md
THIRD_PARTY_NOTICES.md
```

`music`、`confusion`、配额分配与统计尽量写成纯函数，允许注入随机源；UI 不重复实现另一套唱名或计分规则。不必为七个数字引入复杂自适应模型。

项目至少提供：

```bash
npm install
npm run dev
npm run build
npm run typecheck
npm run test
```

测试框架沿用仓库现状；空仓库可采用 Vitest。有条件时使用 Playwright 补充浏览器流程冒烟测试；不为动画增加大量依赖或延迟出题。

## 12. 验收清单

A/B 为共用规则；数字展示及静音继续只适用于简谱识读。C 中混淆计算适用于同模块单场考试，自选强化只属于简谱；D 保留旧数据安全验收，新迁移与听音验收见 12.6。

### 12.1 保留的音乐、音频与通用验收

| 编号 | 验收内容 | 预期结果 |
| --- | --- | --- |
| A01 | C 大调 1、5 | 分别为 do/C4/MIDI60、sol/G4/MIDI67 |
| A02 | D 大调 1、3、5、7 | 唱名 do、mi、sol、si；MIDI62、66、69、73 |
| A03 | E 大调 1、3、7 | 唱名 do、mi、si；MIDI64、68、75 |
| A04 | F♯ 大调 7 | 实际为 E♯5 / MIDI77；听音模块作答前不展示音名 |
| A05 | 十二调全部组合 | 主音加规定半音偏移；各调正确唱名映射相同 |
| A06 | 参数单位 | MIDI69 转为 440Hz 或 A4，不作为 69Hz 播放 |
| A07 | 普通练习/考试覆盖 | 7/21/35 题各音分别 1/3/5 次，不相邻同号 |
| A08 | 答案按钮 | 始终七个唯一唱名，含正确项，顺序独立随机 |
| A09 | 快捷键 | 不存在照抄题面数字即可选对的快捷键或答案编号 |
| A10 | 重听 | 同题同音同顺序，不清零计时、不重复计分、不叠音 |
| A11 | 连点/长按 | 一题仅首次选择生效，下一题及轮次保存去重 |
| A12 | 中断恢复 | 未答题恢复同题、耗时无效；已答题不能重答；交卷前不意外显示答案 |
| A13 | 音频异常 | 加载/播放异常暂停并可重试，仅简谱主动静音可继续；听音必须恢复声音 |
| A14 | 无外网运行 | 依赖和采样准备后，本地服务可完整运行，不请求第三方 CDN |
| A15 | 实际声音 | 人工确认钢琴音色、正确音高，无明显爆音或叠音 |
| A16 | 核心工程 | 类型检查、测试、构建通过，中文 README 命令有效 |

### 12.2 练习与考试模式

| 编号 | 验收内容 | 预期结果 |
| --- | --- | --- |
| B01 | 入口层级 | 首页选模块，各模块再独立选练习/考试，默认题数 21/35 |
| B02 | 练习答错 | 立即显示所选、正确映射；首次答案仍记错 |
| B03 | 考试答错/答对 | 两者提交后均只有中性确认，无正误、颜色、提示音或可访问文本泄露 |
| B04 | 考试中成绩 | 仅显示答题进度，不提前显示分数或正确数 |
| B05 | 完整考试 | 交卷后才展示逐题答案、统计和混淆分析 |
| B06 | 提前交卷 | 确认后按已答题计正确率，标明部分完成，未答不计错 |
| B07 | 零作答 | 无 NaN，无混淆推荐，正确率为“—” |
| B08 | 7 题考试 | 有小样本提示，不推断已稳定掌握 |
| B09 | 模式/调性切换 | 必须结束本轮，新轮次使用新设置，不静默改当前题 |
| B10 | 音频条件记录 | 重听、静音、中断有记录，不混淆有效与无效速度样本 |

### 12.3 混淆识别与可选强化

| 编号 | 验收内容 | 预期结果 |
| --- | --- | --- |
| C01 | 第 7.3 节样本 | 32 对 3 错；3→fa 两次、5→la 一次；无其他虚构方向 |
| C02 | 单向错误 | 3→fa 不自动生成 4→mi 错误；反向无样本与零错误能区分 |
| C03 | 按钮打乱 | 所选 fa 永远计向第 4 列，与屏幕位置无关 |
| C04 | 单次错误 | 可推荐复习但标明单次，不断言长期薄弱 |
| C05 | 全对 | 不生成混淆组，不强制强化，不说永久掌握 |
| C06 | 用户跳过 | 考试保存且可正常离开，无自动倒计时或强制跳转 |
| C07 | 目标勾选 | 可取消部分；全取消时开始强化禁用，离开仍可用 |
| C08 | 目标集合 | 选 3/4 和 5/6 得到去重集合 {3,4,5,6} |
| C09 | 来源调性 | D 大调考试进入强化后仍按 D 大调播放，数字唱名不变 |
| C10 | 对照复习 | 可试听/跳过，不计分；正式答题后隐藏映射卡片 |
| C11 | 强化选项 | 全程七选一随机，不退化为固定二选一 |
| C12 | 单组配额 | 只选 3/4 时，21 题中 3、4 各 8 次，其他各 1 次且无相邻同号 |
| C13 | 多组配额 | 总数 21，目标题共 14、复习题共 7，未选组不进入权重计算 |
| C14 | 全部数字为目标 | 不访问空非目标池，不死循环，正常完成 21 题 |
| C15 | 强化答错 | 即时反馈，不篡改首次提交，不无限追加题目 |
| C16 | 自行结束 | 可提前结束/完成后离开，不要求达到掌握阈值 |
| C17 | 结果分离 | 原考试答案、分数、混淆矩阵不被强化改写 |
| C18 | 再次考试 | 用户主动开始；新 ID、新均衡题序，不用强化队列替代 |
| C19 | 结果解读 | 不直接把强化正确率差值称为能力提升率 |

### 12.4 保存、迁移与异常

| 编号 | 验收内容 | 预期结果 |
| --- | --- | --- |
| D01 | 历史报告 | 重开后能看到原考试错选详情，并再次自选强化 |
| D02 | 多模式历史 | 练习/考试/强化分别标记，考试统计不混入练习 |
| D03 | v1.0 迁移 | 原摘要保留为旧版练习，不伪造具体错选和强化来源 |
| D04 | 存储异常 | JSON 损坏、不可写、未知版本不白屏，不无提示毁掉旧数据 |
| D05 | 保存去重 | 反复进入报告、连续点交卷不会保存重复考试 |
| D06 | 来源被移除 | 强化历史仍可读，返回来源按钮禁用或友好提示 |
| D07 | 刷新进行中页面 | 回首页、不自动播放；先前已交卷的报告保留 |
| D08 | 清空历史 | 需确认，只处理应用自己的存储键 |

### 12.5 视觉与交互验收（0.1.2 更新）

| 编号 | 验收内容 | 预期结果 |
| --- | --- | --- |
| E01 | 整体风格 | 浅灰/纯白、深色文字、单一蓝色强调；无大面积玻璃、渐变、装饰图或仪表盘模板 |
| E02 | 首页入口 | 两个模块同等可发现，模块内再选练习/考试；保留认识音符与历史，无嵌套按钮 |
| E03 | 正式答题 | 简谱以数字为最大焦点；听音无目标数字或音名，七项为操作主体；无答案表或侧栏 |
| E04 | 调性/题数 | 设置页可见且有标签，一轮内锁定；不为美观移除必要选项 |
| E05 | 随机与布局 | 七项每题洗牌，视觉与 DOM 顺序一致；重听和 hover 不重新洗牌 |
| E06 | 焦点公平 | 新题焦点不指向正确项；无数字捷径、固定颜色与选项编号提示 |
| E07 | 即时反馈 | 练习/强化显示文字和对应状态；反馈不遮挡按钮，不改写首次答案 |
| E08 | 考试中性状态 | 相同所选项在答对/答错时外观、提示、过渡时间及可访问文本一致 |
| E09 | 布局稳定 | 题面/答案立即同步更新；反馈预留空间，下一题不因普通反馈跳位 |
| E10 | 报告层级 | 一项主要成绩加完成状态；摘要优先，详情可展开，必需统计没有被删去 |
| E11 | 混淆展示 | 两条正确映射与真实错误方向分开说明，无伪造双向错误 |
| E12 | 强化选择 | 复选框可操作、已选组数明确；暂不强化清晰可见，不自动开始 |
| E13 | 空与异常状态 | 全对、零作答、旧版缺记录、音频错误、保存失败有恰当文案和可用操作 |
| E14 | 小屏与缩放 | 检查第 15.10 节视口和 200% 缩放；无页面横向溢出或被裁掉的功能 |
| E15 | 文字与状态对比 | 正常信息文字目标至少 4.5:1；必要控件/状态至少 3:1，浅色底与焦点也实测 |
| E16 | 键盘/对话框 | Tab/原生激活完整可用、焦点可见；暂停/确认弹窗正确管理焦点和原阶段 |
| E17 | 减少动态效果 | reduce 偏好下无非必要动效；所有状态仍能辨认且计时规则不变 |
| E18 | 本地资源 | 无外部字体请求，不随项目分发 Apple 字体文件；图标来源与许可明确 |
| E19 | 实际视觉材料 | 截图来自实际运行页面并标明状态/视口；未验证项如实列出 |
| E20 | 升级兼容 | v1/v2 备份迁移到 v3；原历史、首次计分及简谱强化保持有效 |

### 12.6 相对音程、双模块和迁移验收

| 编号 | 验收内容 | 预期结果 |
| --- | --- | --- |
| F01 | 两模块入口 | 各自完整练习/考试；设置标题、题页、报告、历史能识别模块 |
| F02 | 每题参考 | 每题先 do 后目标；do-do 同音目标仍发声两次；不只首题给参考 |
| F03 | 非 C 调 | D 大调 do=D4，F♯ 大调 si=E♯5；参考为当前调最低主音，目标不含高 do |
| F04 | 无目标泄露 | 未答题与未交卷考试的可见页面、DOM、data/title/ARIA 均无目标数字、实际音名或正确项提示 |
| F05 | 播放与计时 | 两音不重叠，有释放与间隔；整对结束后才能作答并开始首次计时 |
| F06 | 听音练习 | 首次选择后揭晓唱名和实际音名，无目标数字；答案锁定，错误不改正计分 |
| F07 | 听音考试 | 对错均同一中性确认，交卷后才出现成绩/答案/混淆方向 |
| F08 | 重听 | 同 do、同目标、同选项顺序；整对重播、手动重听计数增加、计时不重置、不排队；答后自动重播不增加该计数 |
| F09 | 中途暂停 | 在参考之后/目标之前暂停，目标不再触发；恢复从 do 重播原题，未答耗时无效 |
| F10 | 取消异步 | 结束、切后台、静音、下一题后旧回调不能恢复答题、发出遗留声音或重存结果 |
| F11 | 静音/失败 | 不能无声计分或静音继续；失败可重试/结束，0 音量启用需明确确认按钮 |
| F12 | 设置独立 | 改听音调性/题数不覆盖简谱；旧设置保留，新模块有明确默认值 |
| F13 | 记录与报告 | 标 module，按模块筛选；同设置再练沿用原模块；只按同轮统计，不混合两个模块成绩 |
| F14 | 迁移 | 原 v2 原始值先备份；逐题/强化快照完整，v1 摘要不伪造；失败保留原键 |
| F15 | 禁止跨模块强化 | 旧简谱 21 题强化通过回归；相对音程不出现或生成简谱强化 |
| F16 | 小屏/键盘/缩放 | 两模块检查 320px、390px、200% 缩放；听音可仅用 Tab/Enter 重听、作答、下一题 |
| F17 | 本地 Mac | 内置采样，独立启动；隐藏/最小化暂停、重启记录保留；原 bundle ID/端口不变 |
| F18 | 版本保留 | 0.1.0 两份文档逐字不改，0.1.2 需求与视觉方案完整且无旧范围冲突 |

涉及随机算法的测试要使用可注入随机源并验证数量、不相邻、集合完整等约束，不靠“随机试几次碰巧没看到某答案”来判错。配额测试要覆盖余数分配、共享端点、多组等权和所有数字都是目标的情况。

音频自动化需核对播放参数、采样请求和暂停恢复；它不等于人工确认实际音色或扬声器发声。Safari/Chrome 真机、无头浏览器、WebKit 自动化分别记录。没有实际听验就保留人工待验项，不声称已听验。

## 13. 交付与实施顺序

建议按以下顺序推进：核查已有代码 → 保留音乐映射和音频能力 → 拆分模式状态 → 均衡出题与首次提交 → 考试报告/混淆分析 → 可选强化与配额 → 历史/迁移 → 异常恢复 → 第 15 节视觉规范落地、响应式与可访问性 → 浏览器验收。

交付完整源码、实际需要的本地钢琴采样与授权说明、依赖锁文件、测试、中文 README、统一视觉变量、实际界面验收材料，以及更新后的 Mac .app 与 ZIP。原样保留两份 0.1.0 文档，新增完整 0.1.2 需求与视觉方案。

README 至少包括：安装启动、两个模块及简谱强化流程、十二调音高约定、考试不即时反馈、混淆统计口径、强化配额与用户自选、声音权限、本地数据及旧版迁移、故障排查、真实浏览器/实际听音验收情况，以及第 15 节视觉、缩放、键盘与减少动态效果的验证情况。

最终汇报列明：已实现内容、实际执行的命令与结果、未完成或未验证项。不能只凭截图认定音频、音乐规则或可选强化流程正确。

## 14. 不在本次范围

不实现小调/教会调式、任意两音的音程名称选择、和弦、绝对音高识别、跟唱录音评分、目标高八度 do、历史机器学习、自适应听音强化、跨设备同步或付费功能。原有简谱强化保留；相对音程本版只有练习和考试。

后续若扩展不同方向或任意两音间距，须另行定义参照与答案集合，不能把本版七唱名题悄悄改成另一种任务。

## 15. 视觉与交互规范：Apple 风格的留白、简洁与秩序

### 15.1 设计定位与范围

用户明确要求：**仿照苹果的视觉审美，留白、简洁。** 本项目的落实方向为：浅色背景、系统字体、清晰层级、充足间距、克制圆角与单一蓝色强调。界面应像一个安静的日常练习工具，而不是营销落地页、后台仪表盘或游戏闯关页面。

参考的是视觉原则，不是照搬某个 Apple 页面。本节所有颜色、尺寸、布局和动画时间都是本项目的设计参数，不声称是 Apple 官方规定。Apple 关于包容性设计的材料强调多种交互方式、可调整的阅读体验和辅助技术支持；本项目因此把易读与可操作放在装饰之前。[V1]

本版完整实现浅色主题；深色主题、换肤、拟真玻璃折射和自定义品牌动画不在本次范围。沿用已有原生 Mac 窗口与网页组件，不引入重型 UI 库。

**视觉调整不改变音乐映射、首次计分、考试反馈和简谱强化配额。0.1.2 因新增模块与独立设置升级为 schema v3，并按第 10 节保留旧数据。数字唯一主角原则用于简谱识读；听音模块不得用数字作题面。**

### 15.2 视觉原则与明确禁止项

**内容优先。** 简谱答题页以数字为第一焦点，七个唱名其次；相对音程没有数字题面，七个唱名是操作主体，中性状态和重听为第二层，进度与退出为辅助。题面不放在有装饰意义的大圆球、插画或钢琴背景上。

**用间距分组。** 先通过对齐、字号和留白表达关系，再考虑分割线与卡片。一个区域最多一层主要卡片容器，不把标题、数字、说明和每项统计都包进不同阴影盒子。

**颜色有含义。** 蓝色用于操作与选中；绿/红只用于允许揭晓正误的场景；普通题面及答案在选择前保持中性。七个唱名不能各有固定的颜色、图标或装饰位置。

**少，但不藏功能。** 首页可以收起次要设置，报告可以展开详情；但“重听”“下一题”“暂不强化”、错误恢复和静音状态必须容易发现。简洁不能变成所有操作都是没有文字的图标。

禁止：彩虹色答案、紫蓝渐变大背景、霓虹发光、大面积磨砂玻璃、连续跳动波形、拟物钢琴键式选项、3D 音符、背景照片、粒子/彩纸/奖杯庆祝、连胜火焰、恐吓式倒计时、密集统计卡、宣传长文、假窗口红黄绿按钮、Apple 标志。首版不做半透明或模糊效果，直接用清晰的纯色层级。

### 15.3 设计变量与色彩

所有通用颜色、间距和圆角集中管理，避免散落的近似色。推荐建立 `src/styles/tokens.css`，已有样式系统可沿用其等价组织方式。

```css
:root {
  color-scheme: light;

  --canvas: #f5f5f7;
  --surface: #ffffff;
  --surface-subtle: #f0f0f3;
  --text-primary: #1d1d1f;
  --text-secondary: #68686d;

  --accent: #0066cc;
  --accent-hover: #0057ad;
  --accent-soft: #eaf2fc;

  --success: #237a3b;
  --success-soft: #eff8f1;
  --error: #b42318;
  --error-soft: #fff1f0;
  --warning: #865600;
  --warning-soft: #fff8e7;

  --border-subtle: #dedee3;
  --control-border: #86868b;
  --focus: #0066cc;
  --disabled-bg: #ececef;
  --disabled-text: #68686d;

  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 24px;
  --space-6: 32px;
  --space-7: 48px;
  --space-8: 64px;

  --radius-control: 12px;
  --radius-card: 20px;
  --radius-dialog: 24px;
  --shadow-card: 0 4px 20px rgb(0 0 0 / 4%);
  --shadow-dialog: 0 16px 48px rgb(0 0 0 / 12%);
}
```

页面和控件不需要用完所有颜色。首页/报告使用浅灰画布、白色内容区域；正式答题页面使用白色画布，不额外套一个巨大的阴影卡片。平面区域默认不加阴影，仅弹窗与必要的首页卡片可使用以上轻阴影。

`--border-subtle` 只用于非必要装饰分隔，不能拿它作为必须辨认的输入框轮廓、复选框状态或焦点标记。需要靠边界识别的控件使用 `--control-border`；焦点、选中与状态色也要有足够对比度。[V3]

普通正文、辅助说明、按钮文字以至少 4.5:1 的前景/背景对比度为验收目标；必要的控件边界与状态标记至少 3:1。这里对大字号正文也统一采用更严格的 4.5:1 项目目标。不能为了“高级灰”把重要文字改成接近背景的浅灰。[V2][V3]

这些变量是候选设计值，仍需核查最终组合，包括浅色状态底、hover、选中、错误、禁用与焦点，而不是只检查主色对纯白背景。锁定已作答选项时不能统一降低整个选项容器的 opacity；应保持唱名和所选项可读。

### 15.4 字体、间距与整体布局

使用本机系统字体，不下载或随项目分发 Apple 字体文件；不依赖 Google Fonts 或其他运行期外部字体请求。推荐字体栈：

```css
font-family: -apple-system, BlinkMacSystemFont, "Segoe UI",
             "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif;
```

字号在实现中优先换算为 `rem`，保持浏览器默认根字号，不通过强制设置根字号阻止用户放大。

| 元素 | 默认尺寸（按 16px 根字号换算） | 字重/行高 | 使用约束 |
| --- | --- | --- | --- |
| 首页主标题 | 32px | 600 / 1.25 | 标题最多两行，不用宣传式超大字 |
| 页面/区域标题 | 22—24px | 600 / 1.35 | 统一层级，不随意跳号 |
| 正文 | 16px | 400 / 1.6 | 中文说明自然换行 |
| 辅助信息 | 14px | 400 / 1.5 | 有效信息不小于此值 |
| 唱名按钮 | 20px | 500 / 1.2 | 七个选项同字号、同字重 |
| 题目数字 | 96—144px，常用 128px | 500 / 1.1 | 数字是最大焦点，避免超细或艺术字体 |
| 报告主成绩 | 56—64px | 600 / 1.1 | 只强调一个核心数字，不出现多组同等大数字 |

题目数字与进度使用 `font-variant-numeric: tabular-nums`，并为题面预留固定的居中容器；即使字体的数字宽度有差异，切换 1 与 7 也不改变整体布局。不要通过给所有中文增加字距来制造“高级感”。

页面宽度：全局内容最大 `960px`；设置最大 `560px`；正式答题最大 `760px`；报告文字与强化选择主列最大 `760px`。水平居中，不使用常驻左侧导航栏或右侧数据面板。

外边距：桌面至少 `32px`，手机至少 `16px`。常用组内间距 `12—16px`，组间 `24—32px`，主要页面区块之间 `40—48px`。留白应随屏幕高度收缩，而不是让用户每道题都滚动找“下一题”。

点击区域：常规操作至少 `44×44 CSS px`，主要开始/继续按钮高 `48px`，答案按钮默认高 `56px`。本项目采用该点击区域下限作为产品设计要求，不将其宣称为所有平台的统一标准。

### 15.5 首页、模块页与设置

顶栏左侧产品名，右侧认识音符与历史记录。首页标题“从一个音开始。”，一句说明“认识唱名，也听见音与音的距离。”。主区两个等权白色入口：

```text
[ 简谱识读                         ] [ 相对音程                         ]
  看数字，听钢琴，选唱名。             先听 do，再辨认目标音的唱名。
  进入模块 →                          进入模块 →
```

模块页显示模块名与一句规则说明，下方等权“练习模式 / 考试模式”。不把四个模式堆成导航后台，不在入口卡内嵌套按钮，不为模块分配不同主题颜色；窄屏入口上下排列。

设置页同时清楚显示模块和模式，调性用标签+下拉，题数用 7/21/35 分段；一个蓝色开始按钮。听音设置写明每题 do → 目标音、目标无数字、必须开启声音，并显示当前参考 do 的实际音名。这个参考音名不是随机目标，允许在设置页说明；正式题页只保留调性。

声音设置可展开，默认音量/静音状态始终可确认。7 题考试用辅助小样本文案，不弹警告。提示开始后锁定设置、未结束轮次重开后不恢复。

### 15.6 简谱识读答题页：数字是唯一主角

顶栏保留产品名/模式与音量控制，不展示历史记录、答案表、认识音符或其他可绕过当前轮次的导航。离开本轮沿用现有结束/提前交卷流程，不能因点击 Logo 而静默丢弃答题记录。

主区由上到下：模式/调性与进度 → 数字题面 → 重听 → 七个答案 → 预留反馈区 → 下一题/结束。

```text
简谱唱名 · 练习                                  音量 30%

D 大调 · 1=D                                     第 8 / 21 题
─────────────────────────────────────────────────────────

                              5

                            重听

      [ mi ] [ si ] [ do ] [ fa ] [ la ] [ sol ] [ re ]

                  （此处为预留反馈空间）

                          [ 下一题 ]

已答 7 题 · 正确 6 题                              结束本轮
```

此图只说明信息层级，不要求用等宽字符构建真实界面。使用自然 HTML/CSS 布局。

**答案排列。** 宽度足够时七个按钮排成一行，按钮默认宽约 `88px`、高 `56px`、间距 `12px`。小于 `768px` 的视口采用第一行四个、第二行三个居中，七个按钮仍等宽，不能让最后一个横跨整行。小于 `480px` 可把间距缩为 `8px`，但不减少选项、不横向滚动找答案。窄屏按钮组最大约 `448px`。

**随机与稳定。** 每题随机的是七个标签所在的位置，不是按钮尺寸、行数或装饰。新题生成后保存一次排列；hover、重听、设置音量、重渲染都不能重新洗牌。DOM 顺序要与视觉顺序一致，不只用 CSS `order` 打乱而让键盘顺序停在原来的 do/re 顺序。

**题面更新。** 下一题的数字和七个答案同时立即呈现，不做逐个飞入、翻卡或交错淡入。正式可作答时再开始计时，不把不可见或过渡中的题目计入操作耗时。

**反馈位置。** 数字、答案、重听与下一题保持原位。预留至少约 `96px` 的反馈区域，未作答时留空，不显示答案暗示。文字放大或反馈换行时允许区域自然长高，不能硬裁剪文字。

**进度。** 可在进度文字下加一条细进度线，使用中性灰或单一蓝色，只表示已提交题数/计划题数，不表示正确率。进度线不做七音彩虹、不分“错题红格”。进度、重听和静音均有文字含义，不只靠图形。

**操作层级。** 重听使用次级按钮，答案默认白底灰边。提交并完成答后重播后才启用蓝色“下一题”；最后一题使用原功能规定的结算文案。结束/提前交卷在同一区域以清晰次级按钮呈现，不使用低对比度小字，也不在考试中显示进行中成绩。

### 15.6.1 相对音程答题页：保留选项，去掉目标数字

沿用白色答题背景、居中最大 760px 主区和同一套答案/反馈/下一题控件。顶栏“相对音程 · 练习/考试”；调性、进度、细进度线维持辅助层级。

原超大数字区域改为约 24px 的普通标题“听音选择唱名”与中性状态：播放时“正在播放参考 do 与目标音…”，结束后“请选择目标音的唱名。”。不得以巨大问号、耳朵插画、波形、唱名首字母、目标点位或色块暗示目标。保留留白，但不设置一个占据半屏的空数字占位。

```text
相对音程 · 练习                              音量 30%
D 大调                                      第 2 / 21 题
────────────────────────────────────────────────────
                     听音选择唱名
                  请选择目标音的唱名。
                  [ 重听 do 与目标音 ]
[ mi ] [ si ] [ do ] [ fa ] [ la ] [ sol ] [ re ]
                 （固定预留的反馈空间）
                        [ 下一题 ]
已答 1 题 · 正确 1 题                          结束本轮
```

只隐藏目标音符数字，不移除进度/音量等必要数值。七个选项不编号、同尺寸、同样式，宽屏一行，小屏四加三。播放、等待和反馈使用同一结构，按钮不能因状态文字变长而跳动。听音练习揭晓唱名/音名但不插回数字；听音考试交卷前始终无解释。重听按钮写明整对声音，不使用只有图标的播放器。

### 15.7 正误、选中、锁定与异常状态

| 状态 | 视觉与文字 | 必须避免 |
| --- | --- | --- |
| 未作答 | 七项中性白底、相同边框 | 正确项独有颜色、阴影或默认焦点 |
| hover / pointer 按下 | 轻微底色或边框变化 | 按钮跳动、位置移动、显著缩放 |
| 键盘聚焦 | 蓝色清晰外描边，建议 3px、外扩 3px | `outline: none` 后没有替代、焦点被裁切 |
| 练习/强化答对 | 正确项用深绿描边；反馈写“正确，5 = sol。” | 只变绿而无文字、烟花或奖励提示音 |
| 练习/强化答错 | 错选项深红、正确项深绿；反馈写明所选与正确关系 | 整页红色、大幅抖动、侮辱或催促文案 |
| 考试已提交 | 所选项统一用蓝色或中性深边框；“答案已记录，请继续下一题。” | 对错使用不同色、勾叉、动画、音效或 ARIA 文本 |
| 答案已锁定 | 保留文字对比度，真实禁用交互 | 整组 opacity 降低到难以辨认 |
| 音色加载 | 短句“正在准备钢琴音色…”；可有小尺寸进度指示 | 虚构百分比、可提前作答或后台计时 |
| 音频异常 | 简短原因与“重试 / 启用声音”操作 | 仅一个红色图标、静默继续考试 |
| 暂停 | 简洁对话框“练习已暂停 / 考试已暂停”，明确“继续” | 后方仍可点击、恢复后换题或重新提交 |

状态反馈应使用正文与可选的小型线性符号，不能只凭颜色传递含义。考试中连隐藏的正确答案文本也不能渲染；中性确认不会因对错改变高度、文案长度或停留时间。

即时反馈按原规则展示当前实际音名，但用第二层文字。例如主行“正确，5 = sol。”，次行“D 大调中的实际音：A4”。考试交卷前不出现该解释。

错误恢复使用原答题状态机，视觉组件不能自行生成新题或重启轮次。确认提前交卷、清空历史等弹窗有清晰标题、简短后果和取消按钮；Escape/取消不得被解释为确认。

### 15.8 考试报告、混淆组与强化

两模块报告都显示模块名，保留同一成绩单层级。以下强化选择/复习/小结仅用于简谱识读；相对音程使用普通文字列表展示“听到 mi 时选成 fa”的方向证据与逐唱名详情，没有强化勾选框。

**考试报告像阅读一张清楚的成绩单，而不是仪表盘。** 顺序为：标题与考试条件 → 一项主要成绩 → 必要摘要 → 文字混淆分析与可选强化 → 可展开的详细记录。

主成绩使用正确率，紧邻显示完成状态与已答/计划题数。示例数据沿用第 7.3 节：`91.4%` 下方写“已答 35 / 35 · 正确 32 · 错误 3”。提前结束必须在主成绩旁清楚标注，不让“只答一题的 100%”看起来像完整考试满分。零作答显示“— / 暂无作答”。

耗时、重听、静音和中断用一组小型文字摘要展示。逐数字统计、逐题记录与 7×7 混淆矩阵可放在可展开详情中；所有必需数据仍可访问，不因简洁而删除。矩阵只使用中性色加单一强度色，并显示数值；窄屏允许该矩阵容器内部滚动，但不能撑宽整个页面。

混淆组使用简洁的白底列表。每一组先展示**两条正确对应**，再解释实际错选方向，例如：

```text
可以对照巩固

☑  3 — mi     /     4 — fa
   本次看到 3 时，2 次选成 fa。
   反方向未发生错误。                     本次重复混淆

☑  5 — sol    /     6 — la
   本次看到 5 时，1 次选成 la。            本次单次错误

已选择 2 组。下一轮共 21 题。

[ 开始所选项强化 ]     [ 暂不强化 ]
```

不要用同等强调的“3 = fa”当标题，也不要用无方向箭头或两边都涂红的图示暗示实际未发生的双向错误。未作答、反方向零错误、旧版无记录的说明沿用原需求，不用“已掌握”替代。

复选框及整行标签容易点击，点击区域至少 44px 高。全部候选组都可查看；默认勾选仍按原规则执行，页面显示已选组数，不使用隐藏默认项。全部取消后禁用开始强化，旁边写“请至少选择一组”，离开仍然可用。

“开始所选项强化”是该区域唯一蓝色实心主按钮；“暂不强化”使用尺寸相近的描边次按钮，文字保持正常对比度，不能缩成页脚细字。“重新考试”“返回首页”可以置于报告底部或标题辅助区，不用三四个蓝色主按钮竞争。

全对时保留报告版式，显示“本次未发现错选项”，不展示空混淆卡、不强推强化。旧版缺少逐题数据时写明“旧版记录没有保存具体选项”，不能把未知显示为没有混淆。

**强化复习页。** 只用简洁卡片展示所选目标的正确数字—唱名，点击试听。主按钮“开始答题”，说明“可以直接开始，不必逐张试听”，返回报告为次操作；不需要两颗语义重复的开始按钮。

**正式强化页。** 复用答题页，仅模式标识改为“强化练习”；移除对照卡片与映射提示，不在侧栏常驻“3=mi、4=fa”。对强化项和全范围复习项使用完全相同样式，不给当前题贴“重点题”提示标签。

**强化小结。** 区分目标项与全体题的统计，来源考试作为文字来源信息；不出现“能力提升 30%”等混淆两个条件的结论。再次强化、返回原报告与重新考试均由用户自行选择。

### 15.9 历史、认识音符与辅助设置

历史页沿用相同宽度与排版，用普通列表而非密集表格墙。每行显示“模块 · 模式 · 调性”“日期 · 完成状态”和正确/已答摘要。模块筛选用“全部模块 / 简谱识读 / 相对音程”，模式筛选用“全部 / 练习 / 考试 / 强化”分段控件（听音模块不显示强化），选中项用同一蓝色或中性深色，不给模式各配永久专属颜色。

点击记录进入既定详情，空历史显示“还没有记录。完成一轮后会保存在这里。”并提供返回入口。保存失败、来源报告已被移除等用就地说明，不能只在自动消失的 Toast 中一闪而过。

认识音符页可以展示 `1—do` 等完整映射和当前实际音名，并试听；这是独立页面，不挂在正在作答的题目旁边。不增加虚拟钢琴键盘以免扩大当前范围。

音量控件可用“音量 30%”按钮展开小面板，面板内有带标签的滑块与静音开关；收起后仍显示“已静音”。图标仅为辅助，使用简单内联 SVG 或仓库已有且许可明确的图标，不使用 emoji 充当核心控件，不捆绑 Apple 专有图标资源。

### 15.10 动效、响应式与可访问性

**动效。** 普通 hover/按下颜色变化约 `120—160ms`；非作答视图切换可采用不超过 `180ms` 的轻微淡入。题目数字与答案更新不加入场动画；按钮不缩放、位移或弹跳。报告数字不滚动计数，不为了动画延后可操作状态。

支持 `prefers-reduced-motion: reduce`，此时取消非必要过渡与旋转指示，用静态文字说明加载状态。这一媒体特性用于响应用户的减少动态效果偏好。[V4]

**响应式。** 以 CSS 布局和内容自然流为主，不用固定绝对坐标拼图，不把页面高度锁死后隐藏溢出。

| 检查视口（CSS px） | 预期表现 |
| --- | --- |
| 1440×900 | 居中窄主区、宽松留白；题面、答案、反馈和下一题无需来回滚动 |
| 1280×720 | 自动收紧上下留白，100% 缩放时完整核心答题区域尽量在一屏内；不能以遮挡代替收紧 |
| 768×1024 | 保持清楚层级，答案可按可用宽度进入紧凑排列 |
| 390×844 | 首页单列；答案四个加三个，均等宽；底部操作不越界 |
| 320×568 | 所有功能可达；允许纵向滚动，禁止页面级横向溢出与隐藏操作 |
| 浏览器 200% 缩放 | 文案可换行、区域可增长；允许纵向滚动，不截断、不把按钮叠到题面上 |

高度不足时先缩小区块间的空白，再缩小超大数字；不要把正文缩小到不可读。首版不使用遮挡内容的固定底部工具条。题面与按钮在同一题中不得因反馈、音量弹层或计数变化跳动。

**键盘与读屏。** 所有操作用语义化控件；图标按钮有中文可访问名称。简谱题目可访问名称为“简谱数字 5”，不能偷加“sol”；听音题目为“听音选择唱名”，不能写目标级数、唱名或实际音名；七个选项按当前视觉顺序被朗读和聚焦。新题焦点移到题目标题或题组容器，而不是正确项；然后按 Tab 进入选项。

反馈区采用适当的 `aria-live="polite"` 提示；考试仅播报中性确认，不广播正确性。下一题后清除上一题反馈和相关可访问文本，不把旧正确答案残留在 DOM 中。对话框打开后管理焦点，取消后回到触发控件；背景不响应答题。简谱识读中声音仍是辅助，保持静音可操作；相对音程是听觉任务，明确要求声音，提供可访问的播放/暂停/错误状态与键盘操作，但不以视觉答案替代听辨，也不宣称不需要听觉。[V1]

禁止通过 CSS 全局移除焦点、禁用浏览器缩放、让菜单只在 hover 下可见，或仅使用动画/声音说明异常。可访问性验收包含文字对比度、控件状态对比度、键盘完整流程及缩放；未测 VoiceOver 就不得写成已兼容通过。

### 15.11 实现与视觉交付

推荐把设计变量、基础控件和页面布局拆开，例如 `tokens.css`、`base.css`、`components.css`、`views.css`；已有小型单文件方案也可以，但同一组件的尺寸、圆角、状态必须统一。样式组织不要求改变第 11 节的技术栈。

先实现首页/设置和正式答题页的完整尺寸与状态，再沿用同一组件完成考试报告、混淆选择、强化复习和历史。不要先安装一个重型后台模板再靠覆盖 CSS 改造成极简风格。

开发期使用固定的演示数据检查报告与错题，不把“演示数据”写入用户真实历史。生产环境不带硬编码的成绩、混淆组或随机错误装饰。

交付时应有：主题变量、可复用按钮/表单/反馈样式、各必需页面、对应功能状态测试；能运行浏览器时保存实际界面的截图，至少覆盖双模块首页/入口、两模块作答前和反馈、听音考试中性确认、各模块报告、原简谱强化、音频异常与手机布局。

截图仅作为实际实现的视觉检查材料，不代替音频、计分、混淆算法或可访问性测试；不能用效果图冒充已经实现的页面。未能启动或未能截图时明确说明，不宣称视觉验收已通过。

## 技术、音乐与视觉参考

以下是实施依据，不代表需要把相应网站嵌入程序。外部资源获取时继续核对实际文件许可与版本。

- [R1] Open Music Theory，Scales and scale degrees。首调唱名、大调音阶及全半音关系：`https://openmusictheory.github.io/scales.html`
- [R2] Tone.js 官方文档。音频启动、Sampler 与钢琴采样示例：`https://tonejs.github.io/`
- [R3] MDN，Web Audio API best practices。用户手势与音频上下文：`https://developer.mozilla.org/en-US/docs/Web/API/Web_Audio_API/Best_practices`
- [R4] MDN，Window: localStorage property。本地存储范围与异常：`https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage`
- [R5] FreePats，Acoustic Grand Piano。Salamander 和 Upright Piano KW 的来源及许可：`https://freepats.zenvoid.org/Piano/acoustic-grand-piano.html`
- [R6] Vite 官方 Getting Started：`https://vite.dev/guide/`
- [R7] MIDI-DDSP 原论文中的 MIDI 编号到频率换算公式：`https://arxiv.org/html/2112.09312v2`

[R1]—[R7] 沿用原需求文档，本次未重新核对全部软件版本与音频资源；实现时继续核查实际依赖接口与许可。以下 [V1]—[V4] 为本次视觉设计补充参考，访问日期为 2026-09-25。颜色、尺寸与页面安排由本项目自行制定，不代表 Apple 官方参数。

- [V1] Apple Developer，WWDC25 — Principles of inclusive app design。多种交互方式、阅读调整与可访问性：`https://developer.apple.com/videos/play/wwdc2025/316/`
- [V2] W3C，Understanding WCAG 2.2 SC 1.4.3: Contrast (Minimum)。文字对比度：`https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html`
- [V3] W3C，Understanding WCAG 2.2 SC 1.4.11: Non-text Contrast。必要控件与状态的非文本对比度：`https://www.w3.org/WAI/WCAG22/Understanding/non-text-contrast.html`
- [V4] MDN，prefers-reduced-motion。响应用户的减少动态效果偏好：`https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@media/prefers-reduced-motion`
