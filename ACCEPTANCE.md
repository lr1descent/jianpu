# 0.1.2 实际验收记录

日期：2026-09-26。依据 `jianpu_solfege_trainer_requirements_v0.1.2.md` 第 12.5、12.6、15 节及 0.1.2 视觉方案。下方保留上一版验收记录，旧记录中的 schema v2 和单模块描述仅属于历史版本。

## 双模块功能与数据

| 项目 | 实际结果 |
| --- | --- |
| 模块入口 | 首页为“简谱识读 / 相对音程”两个等权入口，每个模块分别进入练习或考试 |
| 每题参考音 | 每题先播放当前调原定主音 do，再播放同调 do–si 中的目标音；参考音不额外降八度；重听保持同一对声音 |
| 音源时序 | Chrome 记录真实 `AudioBufferSourceNode.start` 调用：参考音先于目标音；首遍整对声音结束后才解锁答案及计时 |
| 隐藏目标 | 听音题面、未作答 DOM、data 属性、title 与 ARIA 不含目标级数或实际音名；只呈现普通标题、状态与唱名选项 |
| 练习 / 考试区别 | 练习首次提交后揭晓目标唱名；考试提交只显示中性确认，完整七题考试在交卷后才生成逐唱名统计和真实错选方向 |
| 中断 / 重听 | 参考音后暂停取消未触发的目标音；恢复从参考 do 重播同题；重听不重置首次计时；旧回调不能解除新暂停 |
| 声音异常 | 听音模块静音或零音量必须暂停并明确启用声音，不能静音继续；加载重试、暂停恢复通过 |
| 设置 / 历史隔离 | 两模块分别保存调性和题数；历史可按模块及模式筛选，再练使用记录所属模块；相对音程没有简谱强化入口 |
| schema v2 → v3 | 保留原存储键，先备份原始 v2 字符串，再把旧设置和记录归入简谱；保留答案及强化快照，v1 摘要迁移继续支持 |
| 原规则回归 | 原完整 35 题简谱考试和 21 题强化继续通过，音乐映射、首次计分及来源快照规则保持 |
| 旧文档保留 | 两份 0.1.0 文档 SHA-256 与修改前一致；正文中的旧版标识也未改写 |

## 自动检查与真实界面

| 检查 | 结果 |
| --- | --- |
| `npm run test` | 30 / 30 通过：领域 18、音频 5、双模块 5、视觉对比度 2 |
| `npm run typecheck` | 通过 |
| `npm run build` | 通过；最终 Mac 打包时再次完成生产构建 |
| `npm run test:browser` | 24 / 24 Chrome 测试通过：原流程 16、新听音流程 8；使用独立上下文，不写入日常历史 |
| 小屏与键盘 | 原五个指定视口继续通过；听音页在 1280×720、390×844、320×568 检查无页面横向溢出，七项等宽、窄屏四加三排列；键盘作答与下一题可达 |
| 实际视觉检查 | 已查看双模块首页、宽屏听音题、320px 听音反馈及原生 100% / 200% 界面；保持浅色、系统字体、单一蓝色强调、克制圆角，无目标大数字或装饰图形 |
| `npm run package:mac` | 生成 0.1.2 Apple Silicon / macOS 13+ 应用与版本化 ZIP |
| `npm run test:mac -- --network` | 30 项原生静态资源、loopback 服务及端口冲突检查通过；ZIP 解压、ad hoc 签名、arm64 可执行文件及八份钢琴采样哈希通过 |
| WKWebView 实际操作 | 独立 bundle ID `local.jianpu.solfege.qa012` 验证双模块入口、听音练习与考试、播放期间锁定答案、练习反馈及考试中性确认 |
| 原生键盘 / 缩放 | Tab / Enter 开始练习、选择答案、推进下一题；⌘+ 四次至 200%，七项四加三重排，Tab 自动滚动到下一题并成功提交；⌘0 恢复 |
| 原生生命周期 | ⌘H 后出现暂停，继续保留第 2 / 7 题并从参考 do 重播；进行中 ⌘Q 出现退出确认，返回后保持答案且暂停 |
| 原生历史持久化 | 独立副本保存一轮部分练习和一轮部分考试；退出进程、重开后两条历史仍在且模块/模式标注正确；正式应用未写入测试成绩 |

本轮原生拖动缩窗操作未成功：自动化工具返回 `noWindowsAvailable`，重绑应用仍相同，因此不把本轮原生窄窗口记为通过。320px / 390px 小屏验证来自 Chrome，原生重排验证来自真实 200% 页面缩放；上一版原生窄窗口记录继续保留为历史证据。

交付：`artifacts/macos/简谱唱名-0.1.2-macOS-AppleSilicon.zip`。保持正式 bundle ID `local.jianpu.solfege` 与端口 41876，沿用应用历史来源。仅本机 ad hoc 签名，未经 Developer ID 签名与 Apple 公证。

本轮截图保存在 `artifacts/screenshots/v0.1.2/`，旧截图保留。主要截图：[模块首页](artifacts/screenshots/v0.1.2/home-1440x900.png)、[听音题面](artifacts/screenshots/v0.1.2/relative-answering-1280x720.png)、[320px 反馈](artifacts/screenshots/v0.1.2/relative-feedback-320x568.png)、[考试中性确认](artifacts/screenshots/v0.1.2/relative-exam-recorded-1280x720.png)、[考试报告](artifacts/screenshots/v0.1.2/relative-exam-report-1280x720.png)。原生截图和无障碍状态留存在任务操作记录中。

未执行人工扬声器听验、完整 VoiceOver、真实手机、完整 Safari 回归或跨机器 Gatekeeper 验收；自动音源时序与采样校验不等同主观听验。

---

# v1.2 实际验收记录

日期：2026-09-25。实现依据：完整需求第 12 节、第 15 节。截图来自本地运行的真实应用，不是设计效果图；截图中的报告由独立测试上下文作答产生，不写入用户日常历史。

## 已执行

### Mac 打包补充验收（2026-09-25—26）

交付为 `artifacts/macos/简谱唱名.app` 与 `简谱唱名-macOS-AppleSilicon.zip`。实际测试机器为 Apple Silicon、macOS 15.7.9；构建目标为 macOS 13+，未在 macOS 13/14 或 Intel Mac 上运行。原生窗口的截图与无障碍操作证据保留在本任务执行记录中。

| 项目 | 实际结果 |
| --- | --- |
| Swift / AppKit / WKWebView 编译 | 通过；使用 Command Line Tools，不依赖完整 Xcode |
| 打包与解压 | `.app` 可独立打开；ZIP 解压后保留 arm64 可执行权限，内置图标、页面、八份钢琴采样与许可 |
| 签名 | ad hoc 本机签名；解压后 `codesign --verify --deep --strict` 通过；没有 Developer ID 签名或 Apple 公证 |
| 原生资源测试 | 27 项静态响应检查通过；加上实际 loopback 读取、固定来源和端口冲突，共 30 项通过。每个子进程检查设 60 秒上限 |
| 资源完整性 | 解压包八份采样 SHA-256 与 manifest 一致；运行中应用端口返回的八份 MP3 的 MIME、HTTP 状态与 SHA-256 均通过 |
| 真实启动 / 音频就绪 | WKWebView 加载首页，点击开始后从本地采样准备进入答题，未选择静音；不依赖 Vite 5173 开发服务 |
| 原生键盘 | 开启 WKWebView `tabFocusesLinks` 后，Tab / Enter 从首页进入、选择 7 题、选择唱名并推进下一题通过 |
| 原生缩放 | ⌘+ 四次到 200%，七项按四加三重排，Tab 能滚动到答案与下一题并提交；⌘0 恢复 100% |
| 原生小窗口 | 窗口约 391 × 569 像素：报告可纵向滚动；数字、七项四加三排列正常，答题后下一题可聚焦、可见 |
| 生命周期 | 最小化显示暂停；继续后保留第 3 / 7 题及原数字；最终版本 ⌘H 隐藏也显示暂停；进行中 ⌘Q 显示确认，取消保留已有答案 |
| 历史持久化 | 独立验收副本保存一轮部分完成（已答 2 / 7），退出进程重开后历史和小结一致；正式 bundle ID 没有写入测试成绩 |
| 网页回归 | 23 项单元测试、16 项 Chrome 浏览器测试、TypeScript 检查与生产构建通过；新增原生后台通知的状态回归 |

Mac 与浏览器存储独立，未尝试读取或自动迁移 Safari / Chrome 数据。未执行人工扬声器听验、VoiceOver、全平台功能回归或跨机器 Gatekeeper 验收。

### 网页验收（2026-09-25）

| 项目 | 实际结果 |
| --- | --- |
| `npm run test` | 23 / 23 通过（领域、状态、存储 18；音频 3；对比度 2） |
| `npm run typecheck` | 通过 |
| `npm run build` | 通过，包含本地 MP3，未使用外部运行期资源 |
| `npm run test:browser` | Google Chrome，15 / 15 通过；独立临时测试上下文 |
| Chrome 可视窗口 | 实际查看首页、设置、采样加载及答题页；未宣称听验 |
| Safari 可视窗口 | 本地页面加载、采样就绪、选择答案、反馈、后台暂停恢复；通过原生缩放菜单明确设置为 200%，实际推进到第 2 题；恢复 100% |
| Playwright WebKit / 真实手机 / VoiceOver | 未执行；桌面 Chrome 小屏模拟不等同真机 |
| 扬声器人工听验 | 未执行；音色辨识、实际发声和主观爆音仍需人工验收 |

## 第 12.5 节逐项结果

| 编号 | 验证方式与结果 |
| --- | --- |
| E01–E02 | 首页截图与 DOM：浅色、系统字体、蓝色操作；练习/考试独立等权入口，整张卡为一个语义按钮，无嵌套按钮 |
| E03–E04 | 实际答题数字 128px（低高度 96px），选项等尺寸；设置有十二调与三档题数，进入本轮后无可切换设置 |
| E05–E06 | 算法/浏览器验证七项唯一、DOM 顺序一致；重听不改变顺序，数字键不答题，新题聚焦数字标题 |
| E07–E09 | 首次反馈锁定；考试答对/答错只显示同一段中性文案，无 correct/incorrect 样式或答案解释；各视口普通错误反馈前后下一题纵坐标相同 |
| E10–E12 | 报告只有一项大字号正确率并标完成条件，详情可展开；实际运行 35 题示例得 32 对 / 3 错 / 91.4%；勾选组可全部取消，开始禁用但离开可用；主动进入强化 |
| E13 | 已测全对、零作答、采样加载失败后重试、主动静音后恢复、保存失败；旧版/损坏/未知版本/来源移除由单元测试覆盖 |
| E14 | Chrome 五个指定视口及放大文字；Safari 原生页面 200% 缩放完成实际操作。无固定底部遮挡，窄屏允许纵向滚动 |
| E15 | 主题中实际使用的文字/背景组合至少 4.5:1，必要边界、状态与焦点组合至少 3:1；禁用/锁定仍可读。见 `tests/unit/visual.test.ts` |
| E16 | Chrome 键盘从首页进入、选择题数、作答、下一题；焦点环可见；Shift+Tab 在弹窗内循环，Escape 不提交，取消回到触发控件 |
| E17 | Chrome reduced-motion 下答案过渡为 0s；题面本来就没有动画或延时呈现 |
| E18 | 请求检查无第三方 HTTP/font 请求；本地八份采样与许可、系统字体，无外部图标库 |
| E19 | 实际页面截图如下；原生 Safari 200% 的工具截图/无障碍状态留在本任务执行记录，不冒充 Chrome CSS 放大截图 |
| E20 | `schemaVersion: 2`，首次答案/模式/来源独立；v1 摘要迁移和未知版本保留测试通过。本次空项目不存在可直接回归的旧实现或用户历史样本 |

## 视口与布局

| CSS 视口 | 已验证 |
| --- | --- |
| 1440 × 900 | 主区居中，七项一行，答题及下一题在一屏内 |
| 1280 × 720 | 缩小上下留白，核心答题区可达，下一题不需滚动 |
| 768 × 1024 | 七项等宽紧凑排列，没有页面横向溢出 |
| 390 × 844 | 首页单列，七项四加三，等宽，正常错误反馈不移动下一题 |
| 320 × 568 | 四加三，页面无横向溢出；报告、复选项、历史可用；矩阵仅容器内部滚动 |
| Chrome 640 × 450 + 200% 根文字 | 验证额外文字重排，内容能增长、下一题可滚动到达；这项不叫浏览器页面缩放 |
| Safari 原生 200% | 原生菜单显示 200%，大字答题布局与七个选项正常换行，选答、暂停恢复与下一题可执行 |

## 截图索引

所有文件位于 `artifacts/screenshots/`，文件名标示状态及 CSS 视口。

- 首页：[1440 × 900](artifacts/screenshots/home-1440x900.png)、[1280 × 720](artifacts/screenshots/home-1280x720.png)、[768 × 1024](artifacts/screenshots/home-768x1024.png)、[390 × 844](artifacts/screenshots/home-390x844.png)、[320 × 568](artifacts/screenshots/home-320x568.png)。
- 作答前：[1440 × 900](artifacts/screenshots/answering-1440x900.png)。
- 练习答错：[1440 × 900](artifacts/screenshots/practice-wrong-1440x900.png)、[1280 × 720](artifacts/screenshots/practice-wrong-1280x720.png)、[768 × 1024](artifacts/screenshots/practice-wrong-768x1024.png)、[390 × 844](artifacts/screenshots/practice-wrong-390x844.png)、[320 × 568](artifacts/screenshots/practice-wrong-320x568.png)。
- 考试中性确认：[1440 × 900](artifacts/screenshots/exam-recorded-1440x900.png)。
- 考试报告与强化选择：[1440 × 900](artifacts/screenshots/exam-report-selection-1440x900.png)、[320 × 568](artifacts/screenshots/exam-report-selection-320x568.png)。
- 强化复习：[1440 × 900](artifacts/screenshots/reinforcement-review-1440x900.png)。
- 全对报告：[1280 × 720](artifacts/screenshots/all-correct-report-1280x720.png)。
- 音频异常：[1280 × 720](artifacts/screenshots/audio-error-1280x720.png)。
- 历史：[320 × 568](artifacts/screenshots/history-320x568.png)。
- 额外文字放大：[640 × 450、200% 根文字](artifacts/screenshots/text-200-percent-reflow-640x450.png)。

## 音乐、状态与数据的证据边界

纯函数验证覆盖十二调全部 84 个音、A4=440Hz、F♯ 的 E♯ 拼写、均衡洗牌袋、独立选项、最大余数和共享端点配额、所有七音为目标、单组 3/4 各 8 次、真实错选方向、首次计分及中位数。状态测试核查未答中断使耗时无效、已答中断不抹除原记录、重复提交/结算去重。存储测试核查未知数据不覆盖、备份失败、内存提示、上限与来源快照分离。

浏览器完整运行一场 35 题示例考试和一场 21 题强化，核对原考试内容未被修改；历史可重开来源报告并重新选择强化。截图不能替代这些测试。没有执行过的真机/读屏/人工听验项目明确保留待验，不声称全平台验收通过。
